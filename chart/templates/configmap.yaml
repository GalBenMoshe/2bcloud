apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-configmap"
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head><title>Hello, 2bcloud!</title></head>
    <body><h1>(:!</h1></body>
    </html>
